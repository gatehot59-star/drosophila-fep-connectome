# Pins EXACTOS - A-03 de la auditoria externa. Sin rangos, sin ^, sin >=.
#
# Por que exactos y no rangos: un rango convierte "reproducible" en "reproducible
# hasta que alguien publique una version menor". La auditoria conto este repo como
# sin entorno reproducible, y un requirements con rangos no lo arregla.
#
# Las versiones son las MEDIDAS en el container donde corrieron los resultados de
# este repo (brain-env, python 3.12.14), no las ultimas del registry. El paso
# "versiones medidas, no supuestas" del CI las imprime en cada corrida, asi que si
# una de estas no coincide con lo instalado queda registrado y no hay que creerle
# a este comentario.
#
# Lo que este archivo NO cubre, declarado: R 4.5.3, el toolchain
# xtensa-esp-elf-gcc 16.1.0 y torch CPU 2.13 estan en el container y NO se usan
# en el CI. La bateria de guards es python puro mas numpy/scipy, y las tres
# librerias de datos entran porque los instrumentos del repo las importan.

numpy==2.3.5
scipy==1.16.3
pandas==2.3.4
pyarrow==22.0.0
